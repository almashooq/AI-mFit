import 'package:flutter/material.dart';

class Preworkout extends StatelessWidget {
  const Preworkout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}